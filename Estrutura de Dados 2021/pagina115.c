#include <stdio.h>
void main(){
int x, y;
int *p;
/* O proximo comando faz com que p (que é ponteiro
para inteiro) aponte para um float. */
P = &x ;
/* O próximo comando não funciona como esperado. */
y = *p;
return 0;

}
